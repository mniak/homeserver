apiVersion: v1
kind: Service
metadata:
  name: {{ include "homeserver.fullname" . }}
  labels:
    {{- include "homeserver.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "homeserver.selectorLabels" . | nindent 4 }}
